[project]
name = "wtk-e2e"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "pytest>=8.4.0",
  "pyyaml>=6.0.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.pytest.ini_options]
addopts = "-ra"
testpaths = ["."]

[tool.uv]
package = false
